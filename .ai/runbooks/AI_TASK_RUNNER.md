# AI Task Runner

INPUT: Task ID

EXECUTE: load state; load architecture/security/task; inspect repository/database; identify dependencies; define acceptance criteria; implement minimum safe change; verify database/security/user path; run regression tests; collect evidence; update task/state; write handoff.

OUTPUT:

TASK:
STATUS:
IMPLEMENTED:
PROVEN:
UNPROVEN:
FILES:
DATABASE:
SECURITY:
TESTS:
EVIDENCE:
BLOCKERS:
NEXT:

`IMPLEMENTED` and `PROVEN` are separate fields and must never be conflated.
