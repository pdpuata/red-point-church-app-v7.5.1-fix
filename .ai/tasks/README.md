# Task Files

Task definitions may be split into one file per task when a backlog item needs detailed acceptance criteria. The index is `.ai/TASKS.md`.

Each task file must preserve the state machine and distinguish IMPLEMENTED from PROVEN.
